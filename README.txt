#DEPENDENCIES:
- pyaudio (python package)
   -> #pip install pyaudio
   << requires root acces to compile this package so run it as root>>
   << pyaudio requires portaudio.h >>
      -> #apt install portaudio-19.dev
